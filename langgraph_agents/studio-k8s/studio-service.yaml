apiVersion: v1
kind: Service
metadata:
  name: agent-studio
  namespace: rhalexandrini   # replace with your namespace, e.g. imsilva
spec:
  selector:
    app: agent-studio
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
