# Cluster Knowledge Base

*Automatically updated by the Magentic agent after each conversation.*
*Feel free to edit, correct, or delete entries manually.*



---

---
**2026-06-15 17:36**
- Namespace: The cart deployment is typically placed in the default namespace unless overridden in `deploy_cart.yml`.  - Pod naming: Pods will be created from the Deployment defined in `deploy_cart.yml` (likely named `cart` or `cart-service`).  - Service naming: A Kubernetes Service named `cart` (or similar) exposes the cart micro‑service on port 5000 by default.  - File structure: Core files are located under `backend/cart/` – `app.py`, `routes/cart.py`, `models.py`, and `database.py`.  - Container image: Built from a Python 3.11‑slim base with dependencies listed in `backend/cart/requirements.txt`.  - Tracing: OpenTelemetry is configured in `app.py` and can be visualized in Jaeger.  - Database: The service expects a PostgreSQL instance reachable via the `DATABASE_URL` environment variable.  - Inter‑service communication: The cart service only talks to the database; it does not call other micro‑services (e.g., inventory, checkout).  - Deployment patterns: The repository uses Docker Compose for local development (`docker-compose.yml` contains a cart service block) and a separate K8s manifest for production.  - Access limitations: CORS is enabled but only for allowed origins configured in the environment.  - Common patterns: Flask Blueprint (`routes/cart.py`) handles all CRUD endpoints for cart items.  - Known endpoints: `GET /cart/<user_id>`, `POST /cart/<user_id>/items`, `DELETE /cart/<user_id>/items/<item_id>`, `DELETE /cart/<user_id>/items`.  - Recurring troubleshooting point: Ensure the `DATABASE_URL` is correctly set; otherwise, the service fails to connect to PostgreSQL and crashes on startup.

---
**2026-06-15 17:38**
- Cart microservice is built from backend/cart/app.py and uses a Flask Blueprint defined in backend/cart/routes/cart.py.\n- The POST /cart/<user_id>/items endpoint retrieves user_id from session, calls an external product reservation service at PRODUCTS_API_URL, and updates the CartItem in PostgreSQL via SQLAlchemy.\n- OpenTelemetry tracing middleware is enabled; the endpoint sets span attributes for user.id, product.id, quantity, and stock.items.\n- Persistence is handled by PostgreSQL accessed through the DATABASE_URL environment variable.\n- CORS is enabled but limited to allowed origins specified in the environment.\n- Deployment is via kubernetes-manifests/deploy_cart.yml; pod naming convention typically 'cart' or 'cart-service', service named 'cart', exposing port 5000.\n- Known endpoints: GET /cart/<user_id>, POST /cart/<user_id>/items, DELETE /cart/<user_id>/items/<item_id>, DELETE /cart/<user_id>/items.\n- The cart service does not communicate with other micro‑services except for product reservation.\n- Recurring troubleshooting point: ensure DATABASE_URL is correctly set; otherwise the service fails to connect to PostgreSQL and crashes on startup.