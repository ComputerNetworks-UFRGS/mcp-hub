apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-mcp-logs
  namespace: imsilva
spec:
  replicas: 1
  selector:
    matchLabels:
      app: k8s-mcp-logs
  template:
    metadata:
      labels:
        app: k8s-mcp-logs
    spec:
      serviceAccountName: k8s-mcp
      containers:
        - name: k8s-mcp-logs
          image: igormsilva/k8s-mcp-logs:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: MCP_TRANSPORT
              value: "sse"
            - name: MCP_PORT
              value: "8080"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---

apiVersion: v1
kind: Service
metadata:
  name: k8s-mcp-logs
  namespace: imsilva
spec:
  selector:
    app: k8s-mcp-logs
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8081 # Ensure this matches the MCP_PORT in k8s_logs_mcp.py