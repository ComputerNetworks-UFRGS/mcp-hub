apiVersion: v1
kind: Service
metadata:
  name: k8s-mcp
  namespace: imsilva
spec:
  selector:
    app: prometheus-promql-mcp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080 # Ensure this matches the MCP_PORT in prometheus-promql-mcp.py